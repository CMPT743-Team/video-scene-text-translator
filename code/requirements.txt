# Use the split requirements files instead:
#   pip install -r requirements/cpu.txt   (Mac / CPU server)
#   pip install -r requirements/gpu.txt   (GPU server with CUDA 12.1)
-r requirements/base.txt
